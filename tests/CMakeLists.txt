# Enable testing (if you want to use CTest)
enable_testing()

# Add the subdirectories for each test group.
add_subdirectory(constructors)
add_subdirectory(factory)
add_subdirectory(indexing)
add_subdirectory(printing)
